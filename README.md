# Website and Blog

Website built with [11ty](https://www.11ty.dev/) static site generator and hosted on Azure.

## Run Locally

The default port is `8080`. If you need another port use the below:

```bash
npx @11ty/eleventy --serve --port 1234
```